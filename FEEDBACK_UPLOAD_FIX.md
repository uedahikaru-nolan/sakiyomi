# 投稿フィードバック - 動画アップロードエラーの修正

## エラー内容
動画ファイルをアップロードしようとすると、以下のエラーが発生していました:
```
Error: Body exceeded 1 MB limit.
```

## 修正内容

### 1. Next.js設定の更新 (`next.config.mjs`)

Server Actionsのボディサイズ制限を100MBに拡大しました:

```javascript
experimental: {
  serverActions: {
    bodySizeLimit: '100mb', // 動画アップロード用に100MBに設定
  },
},
```

### 2. フォーム送信処理の改善 (`components/features/post-feedback/post-feedback-form.tsx`)

**追加機能:**
- 動画アップロード中は送信ボタンを無効化
- アップロード中に送信しようとすると警告メッセージを表示
- 送信ボタンの表示を状態に応じて変更:
  - 動画アップロード中: "動画アップロード中..."
  - フォーム送信中: "送信中..."
  - 通常時: "フィードバック依頼を送信"

## 開発サーバーの再起動

**重要**: `next.config.mjs` を変更したため、開発サーバーを再起動する必要があります。

```bash
# 開発サーバーを停止 (Ctrl+C)
# 再起動
npm run dev
```

## 動作確認手順

1. 開発サーバーを再起動
2. `/dashboard/post-feedback/new` にアクセス
3. 動画ファイルを選択してアップロード（最大100MB）
4. アップロード完了を待つ
5. 他のフィールドを入力
6. 「フィードバック依頼を送信」ボタンをクリック
7. 正常に送信されることを確認

## 制限事項

- 動画ファイルサイズ: 最大100MB
- 対応フォーマット: video/* (すべての動画フォーマット)
- アップロード先: Supabase Storage (`post-feedback-videos` バケット)

## トラブルシューティング

### エラー: "動画のアップロードが完了するまでお待ちください"

動画アップロード中に送信ボタンを押した場合に表示されます。アップロードが完了するまで待ってから再度送信してください。

### エラー: "動画ファイルは100MB以下にしてください"

100MBを超える動画ファイルをアップロードしようとした場合に表示されます。動画を圧縮するか、別のファイルを選択してください。

### アップロードが遅い場合

- ファイルサイズが大きい場合、アップロードに時間がかかります
- ネットワーク接続を確認してください
- 動画を圧縮してサイズを小さくすることを推奨します
